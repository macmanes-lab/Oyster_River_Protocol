# Oyster River Protocol (python_convert branch)

This repo is worked on from multiple machines, each running its own
Claude Code session with no memory shared between them. Before starting
work, read [NOTES.md](NOTES.md) -- it's the running, dated handoff log of
what changed, why, and what's still open. Add a dated entry there (and
commit it) before wrapping up any substantive session.
