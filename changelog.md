### CHANGELOG

ORP Version 4.0.1-dev7

- **The per-search memory figure is now measured, not inferred.** A single
  diamond search of this input class, run alone on the node with 40 threads,
  peaks at **143.0 GiB** (exit 0, 36m35s, 14.8 core-hours). 670 GiB holds
  four of them and not five. `ORTHOFINDER_GB_PER_QUERY_GB = 96` predicted
  159 GiB and chose 4 -- 11% conservative, and right. No behaviour changes;
  the constant's justification stops being one kernel log line.

- Documented what actually drives the cost: the long-contig tail, not file
  size and not sequence count. The two species that lost searches carry
  9-47x more contigs over 10 kb; the species with the most sequences lost
  none. Recorded with the caveat that bytes remain the proxy the sizing
  uses, and what would be needed to replace it.

ORP Version 4.0.1-dev6

- **Preflight writes down the Slurm job ID, the host, and the diamond
  version.** `sacct` is the only place a finished job's peak memory exists
  and it is keyed on an ID the log never carried, so the run that raised the
  memory question could not afterwards be asked about memory. The log now
  carries the ID and the exact `sacct` line to run. The diamond doing the
  all-vs-all is an unpinned orthofinder dependency rather than the
  `diamond=2.2.5` in orp_env.yml, and several memory fixes in diamond's own
  ChangeLog land in specific versions, so its version is printed too.

ORP Version 4.0.1-dev5

- **diamond's thread count is set through OrthoFinder's config.json, because
  PATH cannot reach it.** `-p 1` is written into the `search_cmd` template in
  `<env>/bin/src/orthofinder/run/config.json`, and OrthoFinder prepends both
  its environment's bin and its own bundled bin at startup -- so the dev4
  shim sat at position 9 of the searches' PATH, behind the real diamond, and
  the searches ran `-p 1` with the shim present and unused. There is no
  `--config` flag, so the install copy is the only place this can be said
  from. ORP now adds a `diamond_orp_<threads>` program beside the stock one
  and selects it with `-S`.

- The stock `diamond` entry is never modified, the original file is backed up
  once as `config.json.orp-backup`, and the entry name carries the thread
  count so runs at different `--cpu` coexist in a file the whole cluster
  shares. Nothing run-specific is written into it. The write is
  temp-and-rename with a read-back and one retry, since two jobs adding
  different entries at once is a lost update.

- If the config cannot be read or written the step falls back to the stock
  program and says what that costs in cores, rather than failing. Verified
  against a missing orthofinder, corrupt JSON, and a config with no diamond
  entry.

ORP Version 4.0.1-dev4

- **The diamond shim was never actually used, and nothing said so.** dev2 put
  the shim on PATH by handing `env=` to subprocess, but `conda run -n X`
  activates the environment and activation *prepends* that environment's own
  bin -- so the shim sat behind `orp_orthofinder/bin`, the real diamond won
  the lookup, and OrthoFinder's `-p 1` stood. Confirmed on the node: four
  diamonds at `NLWP 2`, `%CPU 98`, `-p 1` and no `--tmpdir`, i.e. four cores
  of forty. PATH is now exported *inside* the activated environment, which is
  after conda has had its turn.

- **The step now prints which diamond it resolved**, the same way OrthoFinder
  will resolve it, before the run starts. The failure above was invisible in
  the log for two hours; it is one line now, and a loud one if the shim is
  not what comes back.

ORP Version 4.0.1-dev3

- **Fixes the `TypeError` dev2 introduced in `run_orthofuser`.** `conda_run`'s
  first parameter was named `env`, which is also subprocess's name for the
  environment block, so passing the diamond shim's PATH as `env=` handed the
  method two values for one parameter and the step died at its own call site.
  The parameter is `conda_env` now. Every caller passes it positionally, so
  nothing else changes.

ORP Version 4.0.1-dev2

**Fixes the OrthoFinder all-vs-all losing searches to the OOM killer.**

- **The diamond concurrency cap now binds.** `searches = min(cpu, mem //
  ORTHOFINDER_GB_PER_SEARCH)` was compared against a job count of
  `n_assemblies^2` = 16, and at `--cpu 40 --mem 670` it evaluated to 40 --
  so all sixteen `--more-sensitive` diamonds ran at once and the cap, the
  error messages that pointed at it, and the advice to lower `--cpu` or
  `--max-parallel` were all describing a knob that was not connected. Per-
  search memory is now sized from the largest search input at
  `ORTHOFINDER_GB_PER_QUERY_GB` (96 GB per GB of query, the worst measured
  search), with a 12 GB floor.

- **Lowering concurrency no longer idles the node.** OrthoFinder hands every
  diamond `-p 1` regardless of `-t`, so fewer concurrent searches used to
  mean fewer cores. A generated `diamond` ahead of the real one on PATH
  rewrites `-p` to `--cpu // searches` and points `--tmpdir` somewhere that
  is not OrthoFinder's input directory. Four searches at ten threads is the
  same forty cores as sixteen at one, at a quarter of the peak memory. Only
  the search subcommands are rewritten; `makedb` is untouched.

- **`--orthofinder-searches N`** pins the number of concurrent searches when
  the memory model is wrong for a node.

- The failure messages now name `--orthofinder-searches`, and say that a
  truncated `Blast*.txt.gz` has to be deleted before a resume rather than
  left for OrthoFinder to reuse.

ORP Version 4.0.1 <- 4.0.0

**No change to the assembly or to any score.** Two fixes about the
read-metrics step surviving a real merge, and one about knowing which
pytransrate produced a log.

- **`--mem` now reaches pytransrate.** `run_parallel` splits `--cpu` and
  `--mem` across concurrent steps and hands both to every one of them, but
  `orthotransrate` and `transrate` took the memory figure and dropped it on
  the floor -- the two steps in the pipeline that are actually sized by it.
  pytransrate's read-metrics step allocates per-base coverage accumulators
  for the whole assembly *per worker*: on a 5.8 Gbp merge that is 23 GB
  each, so `-t 40` asks for 928 GB. It caps the workers against a memory
  budget, but with nothing passed it can only find the machine's own free
  memory, so a run given `--mem 670` on a 1.5 TB node was allowed to ask for
  all 928 GB and was OOM-killed after nine hours of mapping and quantifying
  -- then again on each retry, since nothing about a retry made it cap. The
  figure is now forwarded as `--max-memory <mem>G`, and a `--max-memory` or
  `--mem` set in `--pytransrate-args` still wins.

- **Preflight refuses a pytransrate older than 2.2.1** and prints the version
  it found. `orp_env.yml` pins one, but a pin describes the environment as
  built and says nothing about the environment as it is; an env still
  carrying 2.1.0 has neither the memory cap nor the fix that stops a failed
  run deleting its own BAM, and the only trace of that in sixteen hours of
  log is a version number in a banner. It is now the first thing preflight
  says, and being too old stops the run in seconds. A version that cannot be
  read is not fatal -- this catches a stale install, it does not become a new
  way to refuse to start.


ORP Version 4.0.0 <- 3.1.0

**The assembly this pipeline produces changes in this release.** The new scorer computes the four score components, the contig score and the assembly score from the same formulas as the Ruby, but its inputs changed, so contig scores move and -- more importantly -- reorder. `makeorthout` keeps the highest-scoring member of each orthogroup, so that reordering reaches the assembled `.ORP.fasta` itself, not just the reported number: 14.5-19% of contig pairs order oppositely across the boundary. The `TRANSRATE SCORE` lines in `reports/qualreport.<run>` are not comparable to any earlier ORP release's, and the `transrate` entry in `sampledata/benchmarks.md` needs a fresh baseline. pytransrate's `scripts/compare_orthogroup_picks.py` measures exactly which orthogroups change representative between two runs.

- add `chowder.py`, which merges assemblies you already have. Two or more existing transcriptomes plus the read pair they were built from go in; everything `oyster.py` does after its four assemblers -- OrthoFinder, pytransrate scoring, best-of-orthogroup selection, the diamond rescue of what no orthogroup covered, cd-hit-est, salmon, the TPM filter, BUSCO, transrate, strandeval and `qualreport.<run>` -- runs over them, and a `<run>.ORP.fasta` comes out. It is the same code and not a copy: `Chowder` subclasses `Pipeline` and calls the same `merge_and_report()` a full run does, so the selection rule, the group ordering that reaches cd-hit-est's tie-breaks and the scoring cannot drift from ORP's own. Making that sharing possible is the only change to `oyster.py`'s behaviour, and it is confined to one place: the four assembler names, previously spelled out in eight of them, are now an `Assembly` record carried on the pipeline, and the three *different* orders oyster.mk used those four in are named and kept apart (`ASSEMBLY_ORDER` for concatenation, which reaches cd-hit-est through `merged.fasta` and `posthack`; `DIAMOND_PRIORITY` for `build_list5.py`, which keeps the first hit per gene and so ranks the assemblies; `REPORT_ORDER` for the `UNIQUE GENES` lines). Verified inert rather than assumed: the full step graph, every step's declared inputs and outputs, both assembly-reaching orders and the exact `qualreport` line text are unchanged for a four-assembler run. The two differences are that `diamond_uniq` writes its four independent count files in report order rather than the order a dict literal happened to have them in, and that each assembly's diamond step is now declared in the merge half as well -- where, under `oyster.py`, the assembler lanes have already satisfied it and it prints `up to date, skipping`. Three things are specific to a merge: the reads are mandatory (pytransrate scores against them, salmon quantifies against them), with `--corrected-reads` to skip trimming and correction for a pair that has had both; contig names are prefixed with their assembly's label, because two assemblies of one library routinely share contig names and every stage downstream joins on that name; and assembly order, which is significant twice over, is taken out of the user's hands rather than left to the sequence they happened to type. Chowder sorts the assemblies by label and permutes them with a fixed seed, so the order is a function of the *set* of assemblies and not of the command line -- deliberately a seeded shuffle and not a random one, since randomising outright would trade a decision nobody made for one nobody can reproduce, and would let a resumed run disagree with the run it resumed. Colliding labels are numbered by source path for the same reason. The order and its seed are printed at the start of the run and recorded in `assemblies/<run>.ingest.done`; `--seed` measures what the order is worth on real data, and `--assembly-order given` hands the ranking back to anyone who does want to express a preference. Note what this does not do: the picks still depend on the order, and being genuinely order-independent would mean breaking cd-hit-est's ties and the rescue ranking on merit rather than position. Preflight drops SPAdes/Trinity/Trans-ABySS, which a merge never runs, and gains `bwa`, which `strandeval` does run and which nothing had been checking for. A chowder run also says what it is before it does anything else: its own banner -- a bowl rather than `oyster.py`'s shell -- with the version, the command line as run, and a line stating that rnaSPAdes, TransABySS and Trinity were never invoked, printed ahead of preflight rather than after it, so that a missing-tool complaint lands under a banner saying which program is complaining. `reports/qualreport.<run>` says so too, in its `QUALITY REPORT FOR:` header -- which is now written into the file and not only printed, for both entry points -- because a merge's report is otherwise identical in layout to a full run's and is the artefact that outlives the scrollback. The `Command:` line the banner prints and the timing log records is `shlex`-quoted for both entry points, so a read path with a space in it is one argument rather than two and the line can be pasted back to repeat the run
- replace the bundled Ruby `orp-transrate` with [pytransrate](https://github.com/macmanes-lab/pytransrate) 2.2.0, pip-installed into the `orp` env off its pinned `v2.2.0` tag. The Ruby version is pinned to Ruby 2.2.0 (EOL since 2018), ships as an x86_64-linux-only 30MB bundle, and depends on three abandoned binaries; none of that could be carried forward. The CLI is drop-in (`-a`/`-o`/`-t`/`--left`/`--right` unchanged) and the CSV column order is preserved deliberately, so `reportgen`'s positional reads at indices 36/37 of `assemblies.csv` and `scripts/pick_best_contigs.py`'s column 9 of `contigs.csv` are untouched. **Scores are not comparable across this boundary and are not meant to be**: snap-aligner 1.0dev.96 -> 2.0.5 introduced soft clipping, and the old `bam-read` helper advanced its reference cursor over soft-clipped bases as though they consumed reference, shifting a clipped read's coverage contribution rightward -- inert under an aligner that never clipped, corrupting under one that does. Coverage now follows the SAM spec and agrees with `samtools depth`. Measured across three assemblies of one library: `bases_uncovered` rises 3.1-4.5x and the assembly score drops 0.008-0.070, while the good-mapping *rate* is unchanged to +/-0.001 -- the two implementations agree about the reads and disagree about the contigs. Because `makeorthout` keeps the highest-scoring member of each orthogroup, it is contig-score *ordering* and not level that reaches the assembly: 14.5-19% of contig pairs order oppositely, so the final `.ORP.fasta` will differ. pytransrate ships `scripts/compare_orthogroup_picks.py` to measure exactly which orthogroups change representative. The `transrate` entry in `sampledata/benchmarks.md` needs a fresh baseline for the same reason. Install-side this retires the tarball, its `make` target, the Dockerfile `ENV`/`.profile` PATH entries, and INSTALL.md steps 5 and 8 -- a console script inside the env needs none of them. `check()` gains `snap-aligner`, which pytransrate shells out to, so a missing one fails in preflight instead of hours in at `orthotransrate`. Pinned at 2.2.0 rather than 2.0.0, across two bumps whose effects are worth keeping apart. **2.1.0** is a performance release against the same score formulas, so nothing above changes: `-t/--threads` now divides the scoring step as well as snap and salmon, which both ORP call sites already pass `-t <cpu>` to, and the serial work is 1.9x faster per alignment and 5.3x faster per contig. `orthotransrate` is where that lands: it scores the pooled fasta and dominates the merge branch it runs in (26m36s of `merge_branch`'s 26m37s on the SRR1789336 benchmark, Ruby-era timings and so a scale rather than a baseline). Its one numeric change is `p_seq_true` becoming exact: it was the one accumulator carried as a running float, so a parallel run's answer depended on its thread count. It moves by up to 3.3e-15 against 2.0.0 -- the fifteenth decimal, against a `contigs.csv` rounded to six -- and stops being thread-dependent. **2.2.0** is about surviving a real merge, and it is the one that moves scores again, though not through the scorer: `src/pytransrate/score.py` and `src/pytransrate/output.py` are byte-identical across 2.1.0..2.2.0, so the formulas, the CSV column order and pytransrate's `tests/test_output.py` pinning `score`/`optimal_score` at indices 36/37 all stand -- what moves is snap's input. Contig padding drops from 2000 to 1000, which changes the index and so the alignments on every assembly; `--padding` is exposed to override it, and note that the ORP `--pytransrate-args` note below was written against a flag 2.1.0 did not actually have. Deflines containing `|` stop being cut at the pipe, which under 2.1.0 either failed outright or ran to completion with every read metric silently zero -- no concern for oyster.py, whose four assemblers emit no pipes, but a real fix for `chowder.py`, where the assemblies are whatever the user downloaded and ENA and TSA both use them. The rest is why the pin moved at all: the read-metrics step caps its forked processes at memory that actually exists (with `--max-memory`/`--mem` when the detected figure is wrong for the scheduler), `logs/snap.log` is written as snap runs rather than assembled in memory and lost when snap is killed, a snap that dies on a signal says `killed by signal 8 (SIGFPE)` and names amplab/snap#171, the `-locationSize` sweep covers every way snap runs out of genome locations, gzipped assemblies and reads are read directly, and the index survives interruption and concurrent runs behind an `<index>.index.lock`. The CLI is a strict superset of 2.1.0's -- nothing either ORP call site passes was removed -- and `requires-python` and the three runtime dependencies are unchanged, so `orp_env.yml` needed only the tag
- upgrade salmon 2.5.1 -> 2.7.0 in the `orp` env (and add `snap-aligner=2.0.5`/`pysam` for pytransrate, which targets 2.7.0 -- so one env serves both rather than needing a separate `orp_transrate`). Every flag ORP passes was checked against the 2.x migration notes first and nothing breaks: `salmon index` keeps `-t`/`-i`/`-k`/`--threads`, `salmon quant` keeps `-p`/`-i`/`--seqBias`/`--gcBias`/`--libType`/`-1`/`-2`/`-o`, and `quant.sf`'s columns are unchanged so `filter_tpm` still reads TPM from column 4. Dropped `--validateMappings`, which 2.x ignores now that selective alignment is the default -- it was a no-op that read as though it still switched something on. Kept `--no-version-check`, also a no-op, since it stays meaningful against a 1.x salmon. The behavioral change is 2.6.0 making deterministic quantification the default, which lands entirely in the 2.5.1 -> 2.6.0 step (2.7.0 is byte-identical to 2.6.0): TPMs move, and the salmon step stops being a source of run-to-run drift. Downstream this is inert at the default `--tpm-filt 0`, where `filter_tpm` writes the LOWEXP list only when `tpm < 0` and so leaves it empty and `secondfilter` copying through unchanged; only `--tpm-filt > 0` runs see membership shift near the threshold. `orp_trinity`'s own `salmon=1.10.3` is a separate env and is untouched
- fix a resumability hole the salmon upgrade exposed: 2.7.0 requires index format v2 and rejects any index an earlier salmon built, but `needs_run()` only ever compares mtimes -- so resuming a run started under 2.5.1 kept the stale `<run>.ortho.idx`, skipped `salmon_index`, and left `salmon quant` to fail against an index it could not read, once per retry. `stamp_tool_version()` now records the salmon version in `quants/salmon.version` and declares that file as an *input* to `salmon_index`, so a version change reads as an ordinary out-of-date input rather than a special case; the stamp is rewritten only when the version actually changes, or a rebuild would fire every run. `salmon_index` also clears the index directory before rebuilding, since a rebuild here is usually a rebuild over an index salmon has already refused
- clear the output directory before every attempt at `orthotransrate()`/`transrate()`, not once above the retry loop. pytransrate refuses an output directory that already holds an `assemblies.csv` rather than silently overwriting it the way the Ruby did, and `needs_run()` re-runs either step whenever the corrected reads are newer than that csv -- not only when it is absent -- so a resumed run would abort in preflight instead of rescoring. Clearing once was not enough: pytransrate reuses whatever mapping it finds in `-o`, so a step killed part-way leaves a truncated BAM that every later attempt picks back up and dies on with the same missing BGZF EOF marker, having paid for the contig-metrics pass again first. What a retry must not start from, though, is a cold directory, and the first cut of this cleared far too much: everything the clear does not recognise is by definition the work the previous attempt paid for. `clear_transrate_outdir()` therefore asks of each thing in `-o` not whether it is there but whether whatever wrote it finished, and four kinds of thing survive that question. **snap's index**, the most expensive artifact the previous attempt produced -- pytransrate chdirs into `-o`, so the index of `merged.fasta` sits at `merged/merged/`, and on a 5.35M-contig merge it is the better part of an hour, twice that whenever the `-locationSize` sweep steps up from four bytes; any directory carrying snap's own `GenomeIndex` marker is kept, which is what pytransrate keys reuse on, so a partial build (no marker) is still cleared rather than trusted. **The BAM, when it is complete** -- the other multi-hour step, and 159 GB of it on the merge in NOTES.md. 2.2.0 makes the same check before reusing one, but the decision to *keep* the file is ORP's and a truncated BAM is hundreds of gigabytes nothing can use, so the check is made here too: a BAM ending in the empty BGZF block of SAM spec 4.1 was closed by a writer that reached the end and is kept, along with the `.align.done` marker pytransrate reads after 2.2.0 to decide the same thing, the `<index>.index.lock` that belongs beside the index, and the `*-read_count.txt` that saves re-counting the library; one without that block was still being written when snap was OOM-killed, hit its wall clock or died on [amplab/snap#171](https://github.com/amplab/snap/issues/171), and it is deleted, because it is exactly the file every later attempt used to pick back up and die on. **`salmon/`**, but only beside a BAM that was kept -- `quant.sf` is a completed quantification *of that BAM*, so keeping it once the BAM has gone would score the assembly off numbers belonging to a file that no longer exists -- and only when it holds one whole row per contig in the assembly, since salmon writes it in a single pass at the end of its run and pytransrate reuses it on existence alone at every version to date, 2.2.0 included, which would not fail loudly but would quietly score the assembly off whichever contigs made it into the file before the kill. **`logs/`**, by name, because `merged/logs/snap.log` is the file that says why the step failed at all, and a retry that deletes it destroys the evidence it exists to preserve. Everything else goes, including `assemblies.csv`, `contigs.csv` and the score-optimisation csv, which are the outputs being recomputed. Every one of these checks fails towards deleting, so a check that is wrong costs a recomputation and never a wrong score. The `rglob("*.bam")` cleanup loops go at the same time: pytransrate deletes its own BAM on success unless `--keep-bam` is passed
- add `--pytransrate-args` to both entry points: extra arguments as one quoted string, `shlex`-split and appended to both pytransrate invocations, so a value here overrides the same flag passed above it. Both call sites were fixed argument lists, which put the snap index tuning a large merge needs out of reach without editing `oyster.py` -- and asking for it the obvious way got `chowder.py: error: unrecognized arguments: --location-size 5`, which reads as though the flag does not exist rather than as though it belongs to the tool one level down. The one that matters at scale is `--location-size`, which skips snap's sweep when four-byte genome locations are already known not to fit. `--padding` is *not* the second lever it looks like: snap writes padding as N and skips seeds containing N, so it grows the 1 byte/base genome array and nothing else -- measured on a 5.4M-contig, 5.7 Gbp merge, dropping it entirely saved 1% of the branch, and real sequence alone still exceeded the four-byte ceiling
- fix `orthofuser()` writing its sentinel on an OrthoFinder run that computed nothing. OrthoFinder prints its own fatal errors and then exits 0 -- on one 5.35M-contig merge its diamonds were OOM-killed mid-search, and it reported `Blast1_1.txt is corrupted`, `worker reported fatal error` and `An error occurred`, ran for 35,816s, and returned success. `conda_run` was satisfied, `orthofuser.done` was written, and because `needs_run()` only compares mtimes, every subsequent resume skipped the ten-hour step as up to date and handed `makeorthout` whatever `find_orthogroups_txt()` turned up -- so the run went on to pick contigs and build a final assembly from an orthogroup set that was never computed, with nothing downstream able to notice. The sentinel is now gated on the artifact rather than the exit status: a marker is dropped before launching, and a non-empty `Orthogroups.txt` newer than that marker is required, which a stale result from an earlier attempt cannot satisfy. The failure message names the usual cause, since "exited 0 but produced nothing" is not self-explaining. `find_orthogroups_txt()` takes the newest by mtime rather than `rglob`'s first match, which was the same problem's other half: OrthoFinder never reuses a results directory (each invocation makes a fresh `Results_<Mon><Day>`, then `_1`, `_2`), so a working directory that has seen a failed attempt holds several and the order was the filesystem's -- on a resume after a failure, as likely to hand back the attempt that died as the one that succeeded
- cap OrthoFinder's `-t` against memory as well as cores, and forward the branch's memory share into it. `-t` is not just a core count: it is how many `diamond` processes run at once. diamond's default block size is a fixed `-b2.0` and its manual's rule is roughly six times that in GB, so ~12 GB per process regardless of the node (`--more-sensitive` does not change it; only `--very-sensitive`/`--ultra-sensitive` do). `-a` drops to OrthoFinder's own `t/8` default, since the full core count it used to get bought nothing under `-og`, which stops at orthogroups and never reaches the work `-a` parallelises. Worth being precise about what this is and is not: concurrency is capped by the searches that exist, `n_assemblies^2`, before it is capped by anything here -- four assemblies is 16 searches, so ~192 GB, comfortably inside the orthofuser branch's share of a 720 GB node. On anything bigger than a ~200 GB node this knob is structurally incapable of being what OOMs a four-assembly run, and the comment in the code now says so, so that the next person watching a node die looks somewhere other than the searches
- fix `unlink(missing_ok=True)` on the retry-cleanup path, which is 3.8+ against the Python 3.6.8 a cluster's system `python3` still is. It sits on the retry path and nowhere else, so it fired only once a step had already failed, turning a retryable failure into a `TypeError` whose traceback buried the failure that caused it -- and it half-ran first, `rmtree`-ing `logs/` before raising on the first file, so the log that explained the original failure was gone and not one intended file was actually removed
- time every step in `merge_and_report()`, and chowder's `ingest`; nothing passes `timed=False` any more. Leaving eleven bookkeeping steps unrecorded was a fair call when every run was `oyster.py`'s, where each is seconds against an assembler's tens of hours and a line per step would have buried the table. A merge has no assemblers, so the merge half *is* the run: `ingest` rewrites every contig name in every input assembly, and the `list1`-`list7` set algebra scales with the assemblies rather than with the sample data it was last watched on. The `timed=` knob stays on `step()` for a future step genuinely not worth a line
- reclaim intermediate files instead of leaving a finished run's whole working tree on disk. A completed run now keeps `reports/`, `assemblies/<run>.ORP.fasta`, the four individual assemblies and the corrected read pair -- the last two gzipped -- and removes everything else: the trimmed-but-uncorrected reads, `orthofuse/` (OrthoFinder's all-vs-all output plus pytransrate's scoring of the pooled fasta, normally the largest directory in a run), `quants/`, `assemblies/diamond/`, `assemblies/working/`, and the chain of working assemblies between `orthofusing` and `.ORP.fasta`. All of it is reproducible from what's kept, and every number any of it contributed is already in `reports/qualreport.<run>`. `reports/<run>.cleanup.done` records what was kept and what was removed, with sizes. `--keep-intermediates` turns the whole thing off for debugging a run
- fix `run_trinity_phase1`/`run_trinity_phase2` re-running from scratch on a resumed run -- about 35h of wasted compute, and reachable long before this release. Phase 2 passes `--full_cleanup`, which deletes the entire `<run>.trinity/` working directory, including `recursive_trinity.cmds.ok` -- the file that served as both Phase 1's declared output and Phase 2's declared input. A finished Trinity therefore erased its own evidence that it had run: `needs_run()` found Phase 1's output missing and re-ran it (~90min), that rewrote `cmds.ok` *newer* than `<run>.trinity.Trinity.fasta`, and Phase 2 then looked out of date and re-assembled on top of a complete, correct assembly (~34h on the SRR1789336 benchmark). Both phases now hang off `assemblies/<run>.trinity.phase1.done`, written by Phase 1 outside the directory `--full_cleanup` removes. The window is a run whose Stage B succeeded and which then failed or was killed later -- a walltime kill near the end of a 37h run, i.e. precisely when the job gets resubmitted -- so `already_complete()` above does not cover it. `seed_trinity_phase1_sentinel()` back-fills the sentinel for run directories that predate it, taking the mtime of whatever already proves Phase 1 ran (`cmds.ok` if Trinity's working dir is still there, else the finished assembly) rather than stamping `now`, so the first resume after upgrading doesn't hit the bug once on its way out
- delete the `<i>.groups` round-trip between `makegroups` and `pick_best_contigs.py`. `makegroups` split `Orthogroups.txt` into one small file per orthogroup -- of order 1e5 of them into a single directory -- which the picker globbed back in and `makeorthout` then unlinked: three metadata-heavy filesystem passes over data that is written by Python, read by Python and deleted by Python, and only ever crossed a process boundary because the picker runs through `conda run` (it imports nothing outside the stdlib, so it never needed the `orp` env). `scripts/pick_best_contigs.py` now takes `Orthogroups.txt` in place of a directory of `*.groups`, and `makelist`/`makegroups` are gone -- as is `makelist`'s `orthofuse/<run>/<run>.list`, which was written, declared as `makegroups`'s input and never opened by anything. Only the read pass had ever been timed (`makeorthout`, 49s-1m33s on real runs); the write and delete passes were `timed=False`, so on a network filesystem this was invisible cost. **Group ordering is preserved exactly and deliberately**: the old implementation globbed and sorted *filenames*, which is lexicographic (`1, 10, 100, 2, ...`) and not numeric, and that order carries into `good.<run>.list`, contig order in `orthomerged.fasta` and so into `cd-hit-est`, where input order breaks length ties and can change which representative survives into `.ORP.fasta` -- iterating `Orthogroups.txt` in line order instead would have made a performance cleanup an assembly-changing one. `good.<run>.list` is byte-identical across the change on synthetic orthogroup sets from n=1 to n=1111 exercising ties, zero and negative scores, contigs absent from `contigs.csv`, duplicate contig rows and blank lines. pytransrate's `compare_orthogroup_picks.py` is unaffected: it already read `Orthogroups.txt` through `--orthogroups` and rebuilt the groups in memory, precisely because `makeorthout` deleted the files its `--groups` mode wanted. pytransrate 2.1.0 goes further and imports this `pick_best_contigs.py`'s `best_in_group` directly when pointed at an ORP 4.0.0 or later checkout -- the member-list signature the split above gave it -- so the two cannot drift; older checkouts keep its own mirror of the rule
- do the gzipping in the background, starting when each file is finished being *written* rather than when the pipeline is finished *reading* it -- which is much later, since the corrected reads feed every assembler and every alignment step through to `strandeval`, and the four assemblies are read again at `run_filtershort`, `diamond_*` and `posthack`. `compress_async()` queues the corrected pair the moment `run_rcorrector` returns and each assembly the moment its assembler returns, on a two-worker pool, so the compression runs alongside an assembler and `cleanup` at the end has nothing to do but unlink. A file whose background gzip failed or didn't finish is kept uncompressed rather than deleted, and `finish_compression()` joins the pool on every exit path including failure, so a run that dies mid-stage doesn't sit at interpreter exit with no explanation. Adds `pigz=2.8` to the `orp` env for this, with a plain-`gzip` fallback so an env built before it keeps working: a corrected read pair is tens of GB and single-threaded gzip can still be running long after the stage that was meant to hide it has ended
- delete the four `rcorr/<run>.TRIM_*.fastq` inline right after `run_rcorrector` rather than waiting for the end-of-run cleanup. They're the size of the raw input, nothing past read correction ever opens them again, and this is the earliest reclaim the pipeline has -- it frees roughly the corrected pair's own footprint before the assemblers start. `run_trimmomatic` declares a new `rcorr/<run>.trim.done` sentinel as its output in place of the TRIM files whenever the corrected pair is present and current, so a resumed run doesn't read their absence as a reason to re-trim; a working directory from before the sentinel existed still has its TRIM files and skips exactly as it always did
- add `already_complete()`, without which the cleanup above would turn a harmless re-invocation into a destructive one. `needs_run()` decides every step purely from whether its outputs exist and are current, so once cleanup has removed most of them, re-running `oyster.py` on a finished run directory -- a resubmitted cluster job, say -- would find nearly every stage out of date and silently reassemble from scratch over a completed run, where today it no-ops. `main()` now short-circuits on `reports/<run>.cleanup.done` plus an `.ORP.fasta` still newer than the raw reads, printing where the assembly and reports are; it returns before `timing_init()`, which would otherwise truncate the finished run's timing log on the way past. Deleting the marker forces a full re-run in place
- stop creating the `reads/` directory, which `setup()` has been making on every run since the port and which nothing has ever written to
- quiet preflight's per-tool roll call. `check()` printed a `<TOOL> installed` line for each of the dozen-odd binaries it looks for, on every successful run of both entry points; it now says nothing when every tool is present and still exits on the first one that is missing, which is the only thing it ever had to report
- correct `chowder.py`'s `--help`, whose epilog and `--assemblies` text still described the behaviour from before the seeded shuffle -- "merged in the order given; list the one you trust most first". Under the default that is not merely stale but actively misleading: it invites someone to rank their assemblies by typing them in order, which is exactly the decision `--assembly-order` takes away, and they would have no reason to suspect the ranking was being discarded. Both now say what happens and point at `--assembly-order` for the ranking

ORP Version 3.1.0 <- 3.0.0

- split `run_trinity()` into `run_trinity_phase1()`/`run_trinity_phase2()` using Trinity's documented [multi-stage execution support](https://github.com/trinityrnaseq/trinityrnaseq/wiki/Running-Trinity#running-trinity-in-multiple-sequential-stages) (`--no_distributed_trinity_exec`, then a plain re-run that resumes from Phase 1's checkpoints); pair each phase with a different assembler instead of splitting one fixed lane for the whole run: Stage A runs Phase 1 alongside rnaSPAdes55/rnaSPAdes75 at `TRINITY_PHASE1_SHARE=0.25`, Stage B runs Phase 2 alongside Trans-ABySS at `TRINITY_PHASE2_SHARE=0.95`. Real-run evidence (`TIME2_SRR1789336_norm_py_5050parallel`, NOTES.md 2026-08-19) showed Phase 1 finishing in 90min regardless of its CPU share (confirming it's insensitive above Inchworm's own `--inchworm_cpu=10` cap), and Trans-ABySS's 4.5h runtime is ~3.2h single-threaded FASTQ read/graph-build that no CPU count can shrink (traced to `abyss-pe` falling through to the unthreaded `ABYSS` binary whenever neither Bloom-filter mode nor MPI is requested) -- so pairing Trans-ABySS with the dominant, highly-parallel Phase 2 instead of making Phase 2 wait for it reclaims most of what was previously idle Trinity-lane time, without risking SPAdes being starved of cores the way a single shared short-lane reservation would. Trans-ABySS's memory share is not cut along with its reduced CPU share, since its memory footprint doesn't shrink with core count the way SPAdes/Phase 1's do. Validated on a real run (`TIME2_SRR1789336_norm_py_955parallel`, `sampledata/benchmarks.md`): 37:06:19 TOTAL, beating both the no-split baseline (38:28:11, by 1h21m52s) and an even 50/50 split (42:28:36, by 5h22m17s)
- fix `strandeval()`'s `examine_strand.pl` call running bare `perl` instead of going through `conda run -n orp_trinity` like every other subprocess call in the file -- it inherited whatever `PATH` the oyster.py process itself had, and `examine_strand.pl`'s `SAM_reader.pm` shells out to `samtools`, which isn't on `PATH` at all once nothing is conda-activated (see the SLURM-script fix a couple of entries back in NOTES.md). Surfaced as `Can't exec "samtools": No such file or directory` on a real cluster run

ORP Version 3.0.0 <- 2.4.0

- replace oyster.mk (GNU Make) with oyster.py (Python 3.6+), the new primary pipeline entrypoint; every 2.4.0 fix and behavior below (software versions, BUSCO OrthoDB v12.2, NORMALIZE_READS, TPM/cd-hit/diamond-prerequisite fixes, etc.) carries forward unchanged except where noted here
- run independent stages concurrently, capped by the new `--max-parallel` flag (default 2, splitting `--cpu`/`--mem` across however many run at once): the 4 assemblers (Trinity/rnaSPAdes55/rnaSPAdes75/Trans-ABySS); the orthofuser vs. merge/orthotransrate branches; transrate vs. strandeval
- upgrade OrthoFinder 2.5.2 -> 3.1.5
- update software versions: Trinity 2.15.2, SPAdes 4.3.0, BUSCO 6.1.0, DIAMOND 2.2.5, salmon 2.5.1, samtools 1.24, bwa 0.7.19, rcorrector 1.0.7, cd-hit 4.8.1, trimmomatic 0.41, seqtk 1.5, mcl 22.282
- migrate BUSCO lineage database from OrthoDB v10 (eukaryota_odb10) to OrthoDB v12.2 (eukaryota_odb12.2), fetched via BUSCO's own `--download` instead of a hardcoded tarball URL
- update Python to 3.14 in orp_env.yml
- add [INSTALL.md](INSTALL.md): step-by-step install directions covering both the one-command `make` installer and the manual, environment-by-environment alternative, plus how to `mamba env update` an existing `orp` env and the known Python-3.14-solve gotchas above
- run the 4 assemblers as two fixed resource lanes instead of splitting `--cpu`/`--mem` evenly across all of them: Trinity gets its own lane with `TRINITY_LANE_SHARE` (80%) for the whole run, since it dominates wall-clock by orders of magnitude and its thread count is fixed once it launches (no way to hand it more CPU after the other three finish). The remaining 20% runs a second lane -- rnaSPAdes55, rnaSPAdes75, and Trans-ABySS in sequence -- firing each assembly's diamond search immediately after it finishes rather than waiting for the orthofuser/merge stage, which those particular diamond searches never actually depended on. Previously the even split meant Trinity ran alone at half `--cpu` for the last ~36 of its ~37 hours once the other three finished, with the other half of the machine idle
- add NORMALIZE_READS option to oyster.mk, exposing Trinity's `--no_normalize_reads` flag on the command line (defaults to FALSE, preserving prior behavior)


Minor/bugfix

- pin `python=3.14` for `orp_spades` specifically -- fixes SPAdes reporting "Python version 3.6.8 is not supported," a known SPAdes bug (ablab/spades#1319) where it detects a stray system Python instead of its own env's interpreter. Initially pinned across every environment for consistency, but reverted everywhere else after `orp_transabyss`'s solve failed (no non-RC Python 3.14 build available for that dependency set): each conda env's Python is fully isolated from every other env, so there's no real benefit to forcing the newest version outside the one env with an actual bug, and doing so just adds solver risk for older/less-actively-maintained packages
- add file-mtime-based resumability: a step whose output already exists and is newer than its inputs is skipped, so a failed/interrupted run can be re-invoked without recomputing finished work
- deliberately keep diamond, orp_diamond, salmon, and BUSCO sequential at full `--cpu` -- these are CPU-bound tools where splitting cores to enable overlap measured out to no net benefit (diamond) or a real regression (BUSCO, whose ~2 minutes dwarfs the ~13s of transrate+strandeval it would otherwise share cores with)
- add `--version` and full `argparse`-based `--help`
- retry a failed step up to twice (60s apart) before giving up, since a failure on a cluster is often transient (node preemption, filesystem hiccup) rather than a real bug; `run_spades()`/`run_transabyss()` also clean up their non-empty output dir before each retry, since both tools refuse to reuse one rather than resuming (Trinity is exempt from both, since it resumes from its own checkpoints in-place and its ~37h runtime means blindly retrying a deterministic failure could triple the wall time before giving up). Also log immediately when either of the two assembler-lane threads fails, rather than staying silent until the other lane -- often Trinity, tens of hours out -- separately finishes
- add `orp_orthofinder`/`orthofinder` to `check()`'s preflight tool verification, which never covered OrthoFinder before since it used to be a bare downloaded binary rather than a conda env
- remove the `orthofuser.py`-based version-check in the Makefile (`orthopath`/`orthufuserversion`, and the `orthofuser` target's curl/tar logic): dead code left over from ORP's pre-2021 install process, when a MacManes-lab-patched fork of OrthoFinder named `orthofuser.py` supplied nucleotide-input support that upstream OrthoFinder didn't have yet. That patch was retired in 2021 once upstream OrthoFinder added its own `-d` DNA-input flag, but the Makefile's check for it (a path that stopped existing at that point) was never cleaned up until now
- consolidate installed conda environments from 11 down to 5 (`orp`, `orp_spades`, `orp_trinity`, `orp_busco`, `orp_transabyss`) to make install faster and less failure-prone: `orp_rcorrector`, `orp_trimmomatic`, `orp_cdhit`, `orp_diamond`, and `orp_salmon` are folded into the base `orp` env, and `orp_sam` is removed entirely (its samtools was a version-for-version duplicate of `orp`'s, and its bwa/seqtk were never actually used from that env -- every bwa/seqtk call already ran inside `orp_trinity`). `orp_spades`/`orp_trinity`/`orp_busco`/`orp_transabyss` are left fully isolated since each has its own complex or fragile dependency tree -- `orp_trinity` in particular pins an old `salmon=1.10.3` for Trinity's internal isoform filtering, which must never resolve against the pipeline's own modern `salmon=2.5.1` now living in `orp`
- fix orthomerged diamond "unique gene count" rule listing spades55.diamond.txt as a prerequisite twice while omitting spades75.diamond.txt entirely
- fix readcheck's error path so a bad READ1/READ2 (missing file, too-short reads) actually reports its own error message when aborting the build, instead of failing on an unrelated "command not found"
- fix cd-hit-est's memory cap being hardcoded to 5000MB regardless of the run's requested MEM budget
- replace the list3->list5 diamond reconciliation step's shell loop (one grep per gene against the full diamond output) with a single hash-based Python pass, scripts/build_list5.py
- replace the per-orthogroup best-contig selection (one grep of contigs.csv per orthogroup via orthout.done) with a single hash-based Python pass, scripts/pick_best_contigs.py, retiring the orthout.done intermediate
- isolate all orp_* conda env creation from the user's/system's default conda channels
- pin a compatible salmon inside orp_trinity, since an unpinned resolve was picking a salmon 2.x whose Rust rewrite dropped flags Trinity's internal isoform filtering still calls with
- fix the TRANSRATE check to test the bundled binary directly instead of a stale proxy check
- remove dead RCORR/RCORRDIR/BUSCO/BUSCODIR/BUSCODB and TRINITY_KMER variables left over from earlier refactors
- blank out hardcoded /home/ubuntu paths in software/config.ini
- fix strandeval's `hist` histogram call: it was reading unfiltered raw data instead of the filtered numeric column, and separately losing its `-p '#'` argument when re-executed through conda run's internal shell
- fix the software-installed banner (SALMON/BUSCO/SPADES/etc.) printing between every timed step instead of once at the start, caused by per-step timing re-parsing the whole makefile
- fix the BUSCO score in qualreport picking up a line from BUSCO's JSON summary instead of its text summary, after newer BUSCO started writing both a .txt and .json short_summary per run

ORP Version 2.3.2 <- 2.3.1

- update docker base image to Ubuntu 20.04 and new docker image
- update software version for Orthofinder 2,5,2, BUSCO 5.1.2, Trinity 2.12, SPAdes 3.15.2, diamond 2.0.8, RCORRECTOR 1.04
- handle a corner case there TPM filter does not remove any transcripts, which in previous versions caused a crash.
-  change to use mamba during install

ORP Version 2.3.0 <- 2.2.8

- remove Python 2.x dependencies!
- Update BUSCO to 4.0.0 (and to OrthoDB v10 databases)
- Update Spades to 3.14
- Update Trinity to 2.8.5
- Update Orthofuser to be in line with OrthoFinder 2.3.9
- Update the report generation script to use BUSCO4
- Fix bug where crash if TPM_FILT flag was not used.
- Fix bug where 1st low expression transcript was being removed.
- several small bug fixes which were not likely exposed in real datasets.  
